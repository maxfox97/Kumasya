/**
 * Generic, design-independent fallback for unmatched routes. Not a product
 * page — dedicated screens are attached by their own implementation tasks.
 */
export function NotFound() {
  return <p>Page not found.</p>
}
